import { BehaviourSubject } from "../../shared/behaviour-subject/BehaviourSubject";

export const themeSubject = new BehaviourSubject({
  // primaryColor: "rgb(0,224,148)",
  bgPrimaryClassname: "bg-primary-2",
});
